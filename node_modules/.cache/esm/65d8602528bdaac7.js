let User,Course,bcrypt,jwt,dotenv,queryString;_28b‍.x([["default",()=>_28b‍.o]]);_28b‍.w("../models/user",[["default",["User"],function(v){User=v}]]);_28b‍.w("../models/course",[["default",["Course"],function(v){Course=v}]]);_28b‍.w("bcrypt",[["default",["bcrypt"],function(v){bcrypt=v}]]);_28b‍.w("jsonwebtoken",[["default",["jwt"],function(v){jwt=v}]]);_28b‍.w("dotenv",[["default",["dotenv"],function(v){dotenv=v}]]);_28b‍.w("query-string",[["default",["queryString"],function(v){queryString=v}]]);






const Stripe = require("stripe")(process.env.STRIP_SECRET_KEY);



dotenv.config();

const becomeInstructor = async (req, res) => {
  // 1. find user from database
  try {
    const userId = req.auth._id;
    const user = await User.findOne({ _id: userId });

    // 2. if user don't have stripe_ccount_id yet ,then create new
    if (!user.stripe_account_id) {
      const account = await Stripe.account.create({ type: "express" });
      // console.log("account =>", account.id);
      user.stripe_account_id = account.id;
      user.save();
    }

    // 3. create account link based on account id (for frontend to complete onboarding)
    let accountLink = await Stripe.accountLinks.create({
      account: user.stripe_account_id,
      refresh_url: process.env.STRIP_REDIRECT_URL,
      return_url: process.env.STRIP_REDIRECT_URL,
      type: "account_onboarding",
    });
    // console.log(accountLink)

    // 4. pre-fill any info such as eamil (optional), then send ural to front end};
    accountLink = Object.assign(accountLink, {
      "stripe_user[email]": user.email,
    });

    //5. then send the account link response to frontend
    res.send(`${accountLink.url}?${queryString.stringify(accountLink)}`);
  } catch (err) {
    _28b‍.g.console.log("Become instructor error", err);
  }
};

const getAccountStatus = async (req, res) => {
  try {
    const userId = req.auth._id;
    const user = await User.findOne({ _id: userId });
    const account = await Stripe.account.retrieve(user.stripe_account_id);

    if (!account.charges_enabled) {
      return res.status(401).send("Unauthorized");
    } else {
      const statusUpdated = await User.findByIdAndUpdate(
        { _id: userId },
        {
          stripe_seller: account,
          $addToSet: { role: "Instructor" },
        },
        { new: true }
      ).select("-password");
      statusUpdated.password = undefined;
      res.json(statusUpdated);
    }
  } catch (err) {
    _28b‍.g.console.log(err);
  }
};

const currentInstructor = async (req, res) => {
  try {
    const userId = req.auth._id;
    const user = await User.findById({ _id: userId })
      .select("-password")
      .exec();

    if (!user.role.includes("Instructor")) {
      return res.sendStatus(403);
    } else {
      res.json({ ok: true });
    }
  } catch (err) {
    _28b‍.g.console.log(err);
  }
};

const instructorCourse = async (req, res) => {
  try {
    const courses = await Course.find({ instructor: req.auth._id })
      .sort({ createdAt: -1 })
      .exec();

    res.json(courses);
  } catch (err) {
    _28b‍.g.console.log(err);
  }
};

_28b‍.d({
  becomeInstructor,
  getAccountStatus,
  currentInstructor,
  instructorCourse,
});
