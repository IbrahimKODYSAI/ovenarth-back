let express,cookieParser,mongoose,csrf,cors,readdirSync,morgan,dotenv,path;_586‍.w("express",[["default",["express"],function(v){express=v}]]);_586‍.w("cookie-parser",[["default",["cookieParser"],function(v){cookieParser=v}]]);_586‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_586‍.w("csurf",[["default",["csrf"],function(v){csrf=v}]]);_586‍.w("cors",[["default",["cors"],function(v){cors=v}]]);_586‍.w("fs",[["readdirSync",["readdirSync"],function(v){readdirSync=v}]]);_586‍.w("morgan",[["default",["morgan"],function(v){morgan=v}]]);_586‍.w("dotenv",[["default",["dotenv"],function(v){dotenv=v}]]);_586‍.w("path",[["default",["path"],function(v){path=v}]]);








dotenv.config();

const csrfProtection = csrf({ cookie: true });
// express app

const app = express();
app.use(cookieParser());



// connect to db

mongoose
  .connect(process.env.DB_CONNECTION)
  .then(() => {
    console.log("DB connected !");
  })
  .catch((error) => {
    _586‍.g.console.log("DB error", error);
  });

// Middlewares

app.use(cors({ origin: true }));

app.use(express.urlencoded({ extended: true }));

app.use(
  express.json({
    verify: (req, res, buffer) => (req["rawBody"] = buffer),
  })
);
app.use(morgan("dev"));

// Route  middlewares

app.get("/", (req, res) => {
  res.send("we are on home server");
});

readdirSync("./routes").map((route) =>
  app.use("/api", require(`./routes/${route}`))
);

app.use(csrfProtection);

app.get("/api/csrf-token", (req, res) => {
  res.json({ csrfToken: req.csrfToken() });
});

// server listen

const port = process.env.PORT || 3001;

app.listen(port, () => {
  console.log(`server is up and running on port ${port}`);
});
