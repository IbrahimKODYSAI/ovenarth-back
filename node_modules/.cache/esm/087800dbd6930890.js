let jwt,dotenv,User;_d69‍.x([["requireSignin",()=>requireSignin],["isInstructor",()=>isInstructor]]);_d69‍.w("express-jwt",[["expressjwt",["jwt"],function(v){jwt=v}]]);_d69‍.w("dotenv",[["default",["dotenv"],function(v){dotenv=v}]]);_d69‍.w("../models/user",[["default",["User"],function(v){User=v}]]);



dotenv.config();

       const requireSignin = jwt({
  getToken: (req, res) => req.cookies.token,
  secret: process.env.TOKEN_SECRET,
  algorithms: ["HS256"],
});

       const isInstructor = async (req, res, next) => {
  try {
    const userId = req.auth._id;
    const user = await User.findById({ _id: userId });
    if (!user.role.includes("Instructor")) {
      res.sendStatus(403);
    } else {
      next();
    }
  } catch (err) {
    _d69‍.g.console.log(err);
  }
};
